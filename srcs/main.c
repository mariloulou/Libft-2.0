#include <string.h>
#include "../headers/libft.h"

int	main(void)
{
	char *str = "CoucouSalut"
	char *str2 = "CoucouSalut"
	int c = 'A';
	size_t n = 3;

	printf(memset(str, c, n);
	printf(ft_memset(str2, c, n);
	return(0)
}
